// src/server/actions.ts
export const requestMultimodalModel = async () => {
  // 実装は後で追加
};

export const queueTask = async () => {
  // 実装は後で追加
};

export const getTaskStatus = async () => {
  // 実装は後で追加
};
